"""Utility scripts package.

This empty module makes the `scripts` directory a proper Python package so
that scripts can be executed with `python -m scripts.<name>` from the repo
root without modifying `sys.path`.
"""

